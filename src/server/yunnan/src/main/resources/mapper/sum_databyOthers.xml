<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.yunnan.mapper.sum_databyOthers">


    <select id="find_user_bysum_id" resultType="string">
        select user_id from company_info where
        <if test="sum_id == '城市' ">city = #{sum_id};</if>
        <if test="sum_id == 'company_character' ">company_character = #{sum_id};</if>
        <if test="sum_id == 'company_industry' ">company_industry = #{sum_id};</if>
    </select>

    <select id="find_time_id" resultType="String">
        select index_data_table from research_schedule where id between #{start_time} and {end_time}
    </select>



</mapper>